name: 🧪 Tests

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

permissions:
  contents: read

#if (includeCore)
jobs:
  release:
    uses: grovegs/workflows/.github/workflows/package-tests.yml@v1.0.33
    with:
      dotnet-project: tests/{{Author(P)}}.{{Name(P)}}.Tests
      global-json-file: global.json
#endif
