name: 🧪 Tests

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

permissions:
  contents: read

jobs:
  release:
    uses: grovegs/workflows/.github/workflows/package-tests.yml@v1.1.7
    with:
      dotnet-project: tests/{{Author(P)}}.{{Name(P)}}.Tests
      global-json-file: global.json
